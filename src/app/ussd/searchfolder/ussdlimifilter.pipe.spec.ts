import { UssdlimifilterPipe } from './ussdlimifilter.pipe';

describe('UssdlimifilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UssdlimifilterPipe();
    expect(pipe).toBeTruthy();
  });
});
