import { UssdfilterPipe } from './ussdfilter.pipe';

describe('UssdfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UssdfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
