import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'limitincreasereportfilter'
})
export class LimitincreasereportfilterPipe implements PipeTransform {

  transform(value: unknown, ...args: unknown[]): unknown {
    return null;
  }

}
