import { LimitincreasereportfilterPipe } from './limitincreasereportfilter.pipe';

describe('LimitincreasereportfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitincreasereportfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
