import { FundtransferreportfilterPipe } from './fundtransferreportfilter.pipe';

describe('FundtransferreportfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FundtransferreportfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
