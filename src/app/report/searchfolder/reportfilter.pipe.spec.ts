import { ReportfilterPipe } from './reportfilter.pipe';

describe('ReportfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
