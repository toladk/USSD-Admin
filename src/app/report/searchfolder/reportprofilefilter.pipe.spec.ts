import { ReportprofilefilterPipe } from './reportprofilefilter.pipe';

describe('ReportprofilefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportprofilefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
